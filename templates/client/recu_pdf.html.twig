<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Reçu {{ recu.numeroRecu }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            margin: 40px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 3px solid #0d6efd;
            padding-bottom: 20px;
        }
        .header h1 {
            color: #0d6efd;
            margin: 0;
        }
        .info-section {
            margin-bottom: 20px;
        }
        .info-section h3 {
            background-color: #f8f9fa;
            padding: 10px;
            margin-bottom: 10px;
            border-left: 4px solid #0d6efd;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table th {
            background-color: #0d6efd;
            color: white;
            padding: 10px;
            text-align: left;
        }
        table td {
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }
        .total-section {
            text-align: right;
            font-size: 16px;
            font-weight: bold;
            margin-top: 20px;
            padding: 15px;
            background-color: #f8f9fa;
        }
        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 10px;
            color: #666;
            border-top: 1px solid #ddd;
            padding-top: 20px;
        }
        .badge {
            display: inline-block;
            padding: 5px 10px;
            background-color: #28a745;
            color: white;
            border-radius: 3px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🎫 Qataa3.ma</h1>
        <p>Plateforme de Billetterie Événementielle</p>
    </div>

    <div class="info-section">
        <h3>Informations du Reçu</h3>
        <p><strong>Numéro de reçu :</strong> {{ recu.numeroRecu }}</p>
        <p><strong>Date d'émission :</strong> {{ recu.dateRecu|date('d/m/Y à H:i') }}</p>
        <p><strong>Statut :</strong> <span class="badge">Payé (Mode Démonstration)</span></p>
    </div>

    <div class="info-section">
        <h3>Informations Client</h3>
        <p><strong>Nom :</strong> {{ commande.client.prenom }} {{ commande.client.nom }}</p>
        <p><strong>Email :</strong> {{ commande.emailAcheteur }}</p>
    </div>

    <div class="info-section">
        <h3>Détails de la Commande</h3>
        <table>
            <thead>
                <tr>
                    <th>Événement</th>
                    <th>Type de Billet</th>
                    <th>Quantité</th>
                    <th>Prix Unitaire</th>
                    <th>Sous-total</th>
                </tr>
            </thead>
            <tbody>
                {% for ligne in commande.ligneCommandes %}
                    <tr>
                        <td>{{ ligne.typeBillet.evenement.titre }}</td>
                        <td>{{ ligne.typeBillet.nomType }}</td>
                        <td>{{ ligne.quantite }}</td>
                        <td>{{ ligne.prixUnitaire|number_format(2, ',', ' ') }} MAD</td>
                        <td>{{ ligne.sousTotal|number_format(2, ',', ' ') }} MAD</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="total-section">
        <p>MONTANT TOTAL : {{ recu.montantTotal|number_format(2, ',', ' ') }} MAD</p>
    </div>

    <div class="info-section">
        <h3>Informations Importantes</h3>
        <p>✓ Ce reçu a été généré en mode démonstration.</p>
        <p>✓ Aucun paiement réel n'a été effectué.</p>
        <p>✓ Vos tickets sont disponibles dans votre espace client.</p>
    </div>

    <div class="footer">
        <p>Qataa3.ma - Plateforme de Billetterie Événementielle</p>
        <p>Email: contact@qataa3.ma | Tél: +212 6 12 34 56 78</p>
        <p>Document généré le {{ "now"|date('d/m/Y à H:i') }}</p>
    </div>
</body>
</html>
