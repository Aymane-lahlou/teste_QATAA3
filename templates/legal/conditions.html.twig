{% extends 'base.html.twig' %}

{% block title %}Conditions Générales d'Utilisation - Qataa3.ma{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

        :root {
            --primary: #667eea;
            --primary-dark: #764ba2;
            --dark: #0f172a;
            --gray: #64748b;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Inter', -apple-system, sans-serif;
            background: linear-gradient(135deg, #f8fafc 0%, #e8eeff 100%);
            min-height: 100vh;
            padding-bottom: 60px;
        }

        /* ===================== HERO ===================== */
        .legal-hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 100px 0 70px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .legal-hero::before {
            content: '';
            position: absolute;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><circle cx="50" cy="50" r="2" fill="rgba(255,255,255,0.1)"/></svg>');
            animation: movePattern 20s linear infinite;
        }

        @keyframes movePattern {
            from { transform: translateY(0); }
            to { transform: translateY(-100px); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
            max-width: 800px;
            margin: 0 auto;
            padding: 0 20px;
            animation: fadeIn 1s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .hero-icon {
            width: 90px;
            height: 90px;
            background: rgba(255,255,255,0.15);
            backdrop-filter: blur(20px);
            border: 3px solid rgba(255,255,255,0.3);
            border-radius: 25px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 25px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-12px); }
        }

        .hero-icon i {
            font-size: 3rem;
            color: white;
            filter: drop-shadow(0 4px 8px rgba(0,0,0,0.2));
        }

        .legal-hero h1 {
            font-size: 3.2rem;
            font-weight: 900;
            color: white;
            margin-bottom: 18px;
            letter-spacing: -0.03em;
            text-shadow: 0 4px 20px rgba(0,0,0,0.2);
        }

        .hero-date {
            display: inline-block;
            background: rgba(255,255,255,0.2);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.3);
            color: white;
            padding: 10px 24px;
            border-radius: 50px;
            font-size: 0.95rem;
            font-weight: 600;
            box-shadow: 0 8px 24px rgba(0,0,0,0.2);
        }

        /* ===================== NAVIGATION STICKY ===================== */
        .quick-nav-sticky {
            position: sticky;
            top: 0;
            z-index: 1000;
            background: white;
            border-bottom: 2px solid rgba(102,126,234,0.15);
            box-shadow: 0 4px 20px rgba(102,126,234,0.12);
            animation: slideDown 0.5s ease;
        }

        @keyframes slideDown {
            from { transform: translateY(-100%); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 16px 20px;
        }

        .nav-title {
            font-size: 0.85rem;
            font-weight: 800;
            color: var(--dark);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .nav-title i {
            color: var(--primary);
        }

        .nav-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: 8px;
        }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 14px;
            background: transparent;
            border-radius: 10px;
            color: var(--gray);
            text-decoration: none;
            font-size: 0.88rem;
            font-weight: 600;
            transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
            border: 2px solid transparent;
        }

        .nav-link:hover,
        .nav-link.active {
            background: linear-gradient(135deg, rgba(102,126,234,0.1) 0%, rgba(118,75,162,0.1) 100%);
            color: var(--primary);
            border-color: rgba(102,126,234,0.3);
            transform: translateX(3px);
        }

        .nav-link i {
            width: 18px;
            font-size: 1rem;
            color: var(--primary);
        }

        /* ===================== MAIN CONTENT ===================== */
        .legal-main {
            max-width: 900px;
            margin: 40px auto 60px;
            padding: 0 20px;
        }

        .legal-card {
            background: white;
            border-radius: 30px;
            overflow: hidden;
            box-shadow: 
                0 30px 80px rgba(102,126,234,0.15),
                0 0 0 1px rgba(226,232,240,0.5);
            animation: slideUp 0.8s ease 0.2s both;
        }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(40px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* ===================== SECTIONS ===================== */
        .legal-section {
            padding: 60px 50px;
            border-bottom: 1px solid rgba(226,232,240,0.6);
        }

        .legal-section:last-child {
            border-bottom: none;
        }

        .section-header {
            text-align: center;
            margin-bottom: 45px;
        }

        .section-number {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 65px;
            height: 65px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            border-radius: 18px;
            color: white;
            font-size: 1.8rem;
            font-weight: 900;
            margin-bottom: 18px;
            box-shadow: 
                0 12px 35px rgba(102,126,234,0.4),
                inset 0 2px 0 rgba(255,255,255,0.2);
        }

        .section-title {
            font-size: 2.3rem;
            font-weight: 900;
            color: var(--dark);
            margin-bottom: 14px;
            letter-spacing: -0.02em;
        }

        .section-subtitle {
            color: var(--gray);
            font-size: 1.15rem;
            line-height: 1.7;
            max-width: 700px;
            margin: 0 auto;
        }

        .section-content {
            color: var(--gray);
            font-size: 1rem;
            line-height: 1.8;
            margin: 25px 0;
        }

        /* ===================== LISTE ===================== */
        .list-styled {
            list-style: none;
            padding: 0;
            margin: 28px 0;
        }

        .list-styled li {
            position: relative;
            padding-left: 42px;
            margin-bottom: 18px;
            color: var(--gray);
            line-height: 1.8;
            font-size: 0.98rem;
        }

        .list-styled li::before {
            content: '';
            position: absolute;
            left: 0;
            top: 4px;
            width: 26px;
            height: 26px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            border-radius: 8px;
            box-shadow: 0 4px 12px rgba(102,126,234,0.3);
        }

        .list-styled li::after {
            content: '✓';
            position: absolute;
            left: 7px;
            top: 4px;
            color: white;
            font-weight: 900;
            font-size: 0.95rem;
        }

        .list-emoji li::before {
            content: attr(data-emoji);
            background: transparent;
            box-shadow: none;
            font-size: 1.3rem;
            left: 5px;
            top: 0;
        }

        .list-emoji li::after {
            display: none;
        }

        /* ===================== ALERT BOX ===================== */
        .alert-box {
            background: linear-gradient(135deg, #fff9f0 0%, #fff5e8 100%);
            border-left: 5px solid #f59e0b;
            border-radius: 16px;
            padding: 30px;
            margin: 30px 0;
        }

        .alert-box h4 {
            color: #f59e0b;
            font-size: 1.2rem;
            font-weight: 800;
            margin-bottom: 12px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .alert-box p {
            color: var(--gray);
            line-height: 1.7;
        }

        /* ===================== RESPONSIVE ===================== */
        @media (max-width: 768px) {
            .legal-hero {
                padding: 70px 0 50px;
            }

            .legal-hero h1 {
                font-size: 2rem;
            }

            .hero-icon {
                width: 75px;
                height: 75px;
            }

            .hero-icon i {
                font-size: 2.2rem;
            }

            .legal-section {
                padding: 40px 28px;
            }

            .section-title {
                font-size: 1.7rem;
            }

            .nav-grid {
                grid-template-columns: 1fr;
            }

            .nav-container {
                padding: 12px 16px;
            }

            .nav-link {
                padding: 12px 14px;
            }
        }
    </style>
{% endblock %}

{% block body %}
    {# ===================== HERO ===================== #}
    <section class="legal-hero">
        <div class="hero-content">
            <div class="hero-icon">
                <i class="fas fa-file-contract"></i>
            </div>
            <h1>Conditions générales d'utilisation</h1>
            <div class="hero-date">
                <i class="far fa-clock"></i> Dernière mise à jour : {{ "now"|date("d/m/Y") }}
            </div>
        </div>
    </section>

    {# ===================== NAVIGATION STICKY ===================== #}
    <nav class="quick-nav-sticky" id="stickyNav">
        <div class="nav-container">
            <div class="nav-title">
                <i class="fas fa-compass"></i>
                Accès rapide
            </div>
            <div class="nav-grid">
                <a href="#section-1" class="nav-link">
                    <i class="fas fa-bullseye"></i>
                    Objet
                </a>
                <a href="#section-2" class="nav-link">
                    <i class="fas fa-cogs"></i>
                    Services
                </a>
                <a href="#section-3" class="nav-link">
                    <i class="fas fa-user-plus"></i>
                    Inscription
                </a>
                <a href="#section-4" class="nav-link">
                    <i class="fas fa-credit-card"></i>
                    Paiement
                </a>
                <a href="#section-5" class="nav-link">
                    <i class="fas fa-ticket-alt"></i>
                    Billets
                </a>
                <a href="#section-6" class="nav-link">
                    <i class="fas fa-undo"></i>
                    Annulation
                </a>
                <a href="#section-7" class="nav-link">
                    <i class="fas fa-shield-alt"></i>
                    Responsabilité
                </a>
                <a href="#section-8" class="nav-link">
                    <i class="fas fa-gavel"></i>
                    Litiges
                </a>
            </div>
        </div>
    </nav>

    {# ===================== MAIN CONTENT ===================== #}
    <div class="legal-main">
        <div class="legal-card">
            {# Section 1 - Objet #}
            <section id="section-1" class="legal-section">
                <div class="section-header">
                    <div class="section-number">1</div>
                    <h2 class="section-title">Objet</h2>
                    <p class="section-subtitle">
                        Les présentes Conditions Générales d'Utilisation définissent les modalités 
                        d'accès et d'utilisation des services de Qataa3.ma
                    </p>
                </div>

                <div class="section-content">
                    <p>
                        Qataa3.ma est une plateforme de réservation en ligne permettant aux utilisateurs d'acheter 
                        des billets pour divers événements (cinéma, spectacles, concerts, voyages, etc.). 
                        L'utilisation de la plateforme implique l'acceptation pleine et entière des présentes conditions.
                    </p>
                </div>

                <div class="alert-box">
                    <h4><i class="fas fa-exclamation-triangle"></i> Important</h4>
                    <p>
                        En accédant à notre plateforme et en utilisant nos services, vous acceptez sans réserve 
                        les présentes conditions générales d'utilisation.
                    </p>
                </div>
            </section>

            {# Section 2 - Services #}
            <section id="section-2" class="legal-section">
                <div class="section-header">
                    <div class="section-number">2</div>
                    <h2 class="section-title">Services proposés</h2>
                    <p class="section-subtitle">
                        Qataa3.ma met à disposition une plateforme de réservation complète 
                        pour différents types d'événements
                    </p>
                </div>

                <ul class="list-styled list-emoji">
                    <li data-emoji="🎬">Réservation de billets de cinéma avec sélection de sièges</li>
                    <li data-emoji="🎭">Achat de tickets pour spectacles et concerts</li>
                    <li data-emoji="✈️">Réservation de voyages organisés</li>
                    <li data-emoji="🎟️">Billetterie pour événements sportifs et culturels</li>
                    <li data-emoji="📱">Gestion de vos réservations en ligne</li>
                    <li data-emoji="🔔">Notifications pour les événements à venir</li>
                </ul>
            </section>

            {# Section 3 - Inscription #}
            <section id="section-3" class="legal-section">
                <div class="section-header">
                    <div class="section-number">3</div>
                    <h2 class="section-title">Inscription</h2>
                    <p class="section-subtitle">
                        Pour accéder aux services, vous devez créer un compte 
                        avec des informations exactes
                    </p>
                </div>

                <ul class="list-styled">
                    <li>Vous devez être âgé d'au moins 18 ans ou avoir l'autorisation parentale</li>
                    <li>Les informations fournies doivent être exactes et à jour</li>
                    <li>Vous êtes responsable de la confidentialité de vos identifiants</li>
                    <li>Toute activité effectuée via votre compte est sous votre responsabilité</li>
                    <li>Qataa3.ma se réserve le droit de suspendre tout compte suspect</li>
                </ul>
            </section>

            {# Section 4 - Paiement #}
            <section id="section-4" class="legal-section">
                <div class="section-header">
                    <div class="section-number">4</div>
                    <h2 class="section-title">Paiement</h2>
                    <p class="section-subtitle">
                        Toutes les transactions sont sécurisées. Modes de paiement acceptés
                    </p>
                </div>

                <ul class="list-styled">
                    <li><strong>Cartes bancaires :</strong> Visa, Mastercard, American Express</li>
                    <li><strong>Paiement mobile :</strong> Services de paiement mobile marocains</li>
                    <li><strong>Virement bancaire :</strong> Pour les réservations de groupe</li>
                    <li>Les prix affichés sont TTC (Toutes Taxes Comprises)</li>
                    <li>Le paiement est exigible immédiatement lors de la réservation</li>
                    <li>Une confirmation de paiement vous sera envoyée par email</li>
                </ul>

                <div class="alert-box">
                    <h4><i class="fas fa-lock"></i> Sécurité</h4>
                    <p>
                        Vos informations de paiement sont cryptées et sécurisées. Nous ne conservons 
                        jamais vos données bancaires complètes.
                    </p>
                </div>
            </section>

            {# Section 5 - Billets #}
            <section id="section-5" class="legal-section">
                <div class="section-header">
                    <div class="section-number">5</div>
                    <h2 class="section-title">Billets et réservations</h2>
                    <p class="section-subtitle">
                        Modalités de délivrance et d'utilisation des billets
                    </p>
                </div>

                <ul class="list-styled">
                    <li>Les billets sont envoyés par email au format PDF après confirmation du paiement</li>
                    <li>Chaque billet contient un QR code unique pour l'accès à l'événement</li>
                    <li>Les billets sont nominatifs et ne peuvent être revendus sans autorisation</li>
                    <li>Vous devez présenter le billet (numérique ou imprimé) ainsi qu'une pièce d'identité</li>
                    <li>Les billets perdus ou volés ne peuvent pas être remplacés</li>
                    <li>Le partage ou la duplication de billets est strictement interdit</li>
                </ul>
            </section>

            {# Section 6 - Annulation #}
            <section id="section-6" class="legal-section">
                <div class="section-header">
                    <div class="section-number">6</div>
                    <h2 class="section-title">Annulation et remboursement</h2>
                    <p class="section-subtitle">
                        Politique d'annulation et conditions de remboursement
                    </p>
                </div>

                <ul class="list-styled">
                    <li><strong>Annulation par le client :</strong> Possible jusqu'à 48h avant l'événement avec frais de 15%</li>
                    <li><strong>Annulation par l'organisateur :</strong> Remboursement intégral automatique</li>
                    <li><strong>Report d'événement :</strong> Vos billets restent valables pour la nouvelle date</li>
                    <li>Les demandes de remboursement doivent être faites via votre compte</li>
                    <li>Le remboursement est effectué sous 7 à 14 jours ouvrables</li>
                    <li>Certains événements peuvent avoir des politiques spécifiques (non remboursables)</li>
                </ul>

                <div class="alert-box">
                    <h4><i class="fas fa-info-circle"></i> À noter</h4>
                    <p>
                        Les frais de service ne sont jamais remboursables, sauf en cas d'annulation 
                        de l'événement par l'organisateur.
                    </p>
                </div>
            </section>

            {# Section 7 - Responsabilité #}
            <section id="section-7" class="legal-section">
                <div class="section-header">
                    <div class="section-number">7</div>
                    <h2 class="section-title">Limitation de responsabilité</h2>
                    <p class="section-subtitle">
                        Qataa3.ma agit en tant qu'intermédiaire entre les utilisateurs 
                        et les organisateurs d'événements
                    </p>
                </div>

                <ul class="list-styled">
                    <li>Qataa3.ma ne peut être tenu responsable de l'annulation ou du report d'événements</li>
                    <li>La qualité des événements relève de la responsabilité exclusive des organisateurs</li>
                    <li>Nous ne garantissons pas la disponibilité continue de la plateforme</li>
                    <li>L'utilisateur est responsable de la vérification de ses informations de réservation</li>
                    <li>En cas de force majeure, notre responsabilité ne peut être engagée</li>
                </ul>
            </section>

            {# Section 8 - Litiges #}
            <section id="section-8" class="legal-section">
                <div class="section-header">
                    <div class="section-number">8</div>
                    <h2 class="section-title">Droit applicable et litiges</h2>
                    <p class="section-subtitle">
                        Résolution des différends et juridiction compétente
                    </p>
                </div>

                <div class="section-content">
                    <p>
                        Les présentes conditions sont régies par le droit marocain. En cas de litige, 
                        une solution amiable sera recherchée avant toute action judiciaire. 
                        À défaut d'accord amiable, les tribunaux de Casablanca seront seuls compétents.
                    </p>
                </div>

                <ul class="list-styled">
                    <li>Médiation préalable recommandée via notre service client</li>
                    <li>Conservation des preuves (emails, billets, confirmations)</li>
                    <li>Délai de réclamation : 30 jours après l'événement</li>
                    <li>Contact : <a href="mailto:support@qataa3.ma" style="color: #667eea; font-weight: 600;">support@qataa3.ma</a></li>
                </ul>
            </section>
        </div>
    </div>

    <script>
        // Smooth Scroll
        document.querySelectorAll('.nav-link').forEach(link => {
            link.addEventListener('click', function(e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    const offset = 120;
                    const targetPosition = target.offsetTop - offset;
                    window.scrollTo({ top: targetPosition, behavior: 'smooth' });
                }
            });
        });

        // Active Link on Scroll
        const sections = document.querySelectorAll('.legal-section');
        const navLinks = document.querySelectorAll('.nav-link');

        window.addEventListener('scroll', () => {
            let current = '';
            sections.forEach(section => {
                const sectionTop = section.offsetTop - 150;
                if (pageYOffset >= sectionTop) {
                    current = section.getAttribute('id');
                }
            });

            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href').substring(1) === current) {
                    link.classList.add('active');
                }
            });
        });
    </script>
{% endblock %}
